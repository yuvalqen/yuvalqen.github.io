<tr${' class="hidden-row"' if index and int(index) > 1 else ''}>
    <td>
        <label for="field_${name}"">${name}</label>
    </td>
    <td>
    % if type == 'input':
        % if index:
            <input id="field_${name}_${index}" name="${name}_${index}" ${'required ' if required else ''}placeholder="${name} ${index}" type="text" />
            </td><td>
            <input id="field_${name}_${index}_perc" class="category-perc" name="${name}_${index}_perc" ${'required ' if required else ''}type="number" step=10 value="${100 if int(index) == 1 else 0}" />%
            % if int(index) > 1:
                </td><td><button class="btn btn-default add-remove-btn remove-category" data-index=${index}></button>
            % endif
        % else:
            <input id="field_${name}" name="${name}" ${'required ' if required else ''}placeholder="${name}" type="text" />
        % endif
    % elif type == 'date':
        <input id="field_${name}" name="${name}" ${'required ' if required else ''}placeholder="${name}" type="date" onchange="updateDates(this)" onfocus="this.showPicker()" />
    % elif type == 'select':
        <select id="field_${name}" name="${name}">
            % for option in options.split(','):
                <option value="${option}">${option}</option>
            % endfor
        </select>
    % elif type == 'textarea':
        <textarea id="field_${name}" name="${name}" ${'required ' if required else ''}placeholder="${name}"></textarea>
    % elif type == 'number':
        <input id="field_${name}" name="${name}" ${'required ' if required else ''}placeholder="${name}" type="number" step=".01" />
    % elif type == 'file':
        <input id="field_${name}" name="${name}" ${'required ' if required else ''}placeholder="${name}" type="file" /><input type="hidden" name="${name}_content" /><input type="hidden" name="${name}_mimeType" />
    % endif
    </td>
</tr>